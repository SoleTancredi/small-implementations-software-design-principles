/**
 * in case we want to add more figures in the future, we will only
 * have to implement this interface, without modifying the Presentation class
 */
public interface IFigura {
    float area();
}
